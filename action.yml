name: 'PureScript Doc Check'
description: 'Compile all PureScript docs in your poject'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: 'node12'
  main: 'dist/index.js'
